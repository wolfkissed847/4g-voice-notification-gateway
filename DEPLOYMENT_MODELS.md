# รูปแบบการใช้งานระบบ: ใช้เอง vs เปิดให้คนอื่นใช้

เอกสารนี้แยกกรณีการใช้งาน 2 รูปแบบ วิเคราะห์ว่าโค้ดปัจจุบันรองรับอะไรได้แล้ว
อะไรยังขาด และควรลงมือทำอะไรตอนนี้ อะไรรอได้

อัปเดต: 26 ก.ค. 2569 — อ้างอิงโค้ดที่ schema revision `76a340f49b9e`

---

## สรุปคำตอบก่อน

**ควรรองรับทั้ง 2 แบบ แต่ไม่ใช่การ "ทำ 2 ระบบ"** — สิ่งที่ควรทำตอนนี้คือ
**ขีดเส้นแบ่งเจ้าของข้อมูล (tenant) ลงใน data model เลย** แล้วให้รูปแบบ A
เป็นแค่กรณีที่มี tenant เพียงรายเดียว

เหตุผล: การเติมคอลัมน์ `tenant_id` ตอนนี้ที่ตารางยังว่างเปล่า ใช้เวลาไม่นาน
แต่ถ้าปล่อยไว้จนมีข้อมูลจริง + endpoint 25 ตัวเขียนเสร็จหมดแล้ว การย้อนกลับมาใส่
ต้องแก้ทุก query ทุก endpoint พร้อม backfill ข้อมูลเก่าที่ไม่รู้ว่าเป็นของใคร

ส่วนที่**ยังไม่ต้องทำ**คือ login แยกรายลูกค้า, ระบบคิดเงิน, quota — พวกนี้รอจนมีลูกค้ารายที่สองจริงได้

---

## 1. สองรูปแบบต่างกันตรงไหน

| | **แบบ A: ใช้เอง (single-tenant)** | **แบบ B: เปิดให้คนอื่นใช้ (multi-tenant)** |
|---|---|---|
| เราเป็นใคร | บริษัท IoT ที่มีโหนดของตัวเองกระจายอยู่ | ผู้ให้บริการ API แจ้งเตือน |
| ใครยิง `/notify` | โหนดของเราเอง หลายจุด | โหนดของลูกค้า หลายเจ้า |
| โทรไปหาใคร | ทีมงานของเราเอง | ทีมงานของลูกค้าแต่ละราย |
| ใครกำหนดเบอร์ | เราเอง | ลูกค้า (หรือเรา config ให้) |
| ค่าโทรใครจ่าย | เราเอง = ต้นทุนของเรา | เราจ่ายก่อน ต้องเก็บจากลูกค้า |
| ความเสียหายถ้าคิวยาว | งานของเราเองช้า | **ผิดสัญญากับลูกค้า** |
| ข้อมูลส่วนบุคคล | เบอร์พนักงานเราเอง | **เบอร์พนักงานบริษัทอื่น** |

จุดที่ต่างกันจริงๆ ไม่ใช่ "จำนวนคนที่ยิงเข้ามา" — แบบ A ก็มีหลายโหนดยิงเข้ามาอยู่แล้ว
และโค้ดตอนนี้รองรับได้สบาย จุดที่ต่างคือ **ข้อมูลของแต่ละเจ้าต้องไม่ปนกัน** และ
**ทรัพยากรที่มีชิ้นเดียว (ซิม) ต้องถูกแบ่งอย่างเป็นธรรม**

---

## 2. แบบ A — สิ่งที่มีอยู่ตอนนี้

โค้ดปัจจุบันคือแบบ A เต็มตัว และทำงานได้ตามที่ออกแบบ:

- โหนดหลายจุดยิง `POST /notify` พร้อม API key ได้ไม่จำกัดจำนวน
- แยกประเภทเหตุการณ์ได้ (`event_types`) → ผูกกับกลุ่มผู้รับ (`groups` + `contacts`)
- สร้าง API key ได้หลายอันจาก dashboard แยกตามโหนด/ระบบที่ยิงเข้ามา
- ผู้ดูแลคนเดียว login เข้ามาปรับ config ได้ทุกอย่าง

**ที่ต้องทำเพิ่มสำหรับแบบ A ไม่มีเรื่อง tenant เลย** — งานที่เหลือคือ P0/P1 ตามแผนเดิม
(escalation chain, `stream_audio()`, error handling ของ worker) ซึ่งจำเป็นทั้งสองแบบอยู่แล้ว

---

## 3. แบบ B — แยกย่อยได้ 2 ระดับ

ความต่างของต้นทุนระหว่าง 2 ระดับนี้ห่างกันมาก ต้องเลือกให้ชัด

### B1: Managed (เรา config ให้ลูกค้า)

ลูกค้าไม่ได้ login เข้าระบบ ส่งเบอร์และประเภทเหตุการณ์มาให้เรา
เรากรอกให้จาก dashboard ของเราเอง แล้วส่ง API key กลับไป

- Dashboard ยังมีผู้ใช้คนเดียว (เรา) — **ไม่ต้องทำระบบ user เลย**
- ต้องมีการแบ่ง tenant ในข้อมูล เพื่อกัน key ของลูกค้า A ไปสั่งโทรหาลูกค้า B
- เหมาะกับลูกค้าไม่กี่ราย และเหมาะกับขนาดของโปรเจคสหกิจ

### B2: Self-service (ลูกค้า login จัดการเอง)

ลูกค้าเข้ามาแก้เบอร์/กลุ่ม/event type/ดูประวัติของตัวเองได้

- ต้องมีตาราง `users` จริง + role (platform admin vs tenant user)
- ต้องใส่การกรอง tenant ที่ **ทุก endpoint** ไม่ใช่แค่ `/notify`
- ต้องมีหน้าจัดการ tenant, quota, และควรมีระบบคิดเงิน
- งานหนักกว่า B1 หลายเท่า — ไม่ควรทำจนกว่าจะมีลูกค้าจริงหลายราย

---

## 4. ช่องโหว่ที่ทำให้แบบ B ใช้ไม่ได้ตอนนี้

ทั้งหมดนี้ตรวจจากโค้ดจริง ไม่ใช่การคาดเดา

### 4.1 API key ไม่ผูกกับใครเลย — ช่องโหว่ที่ร้ายแรงที่สุด

[main.py:64](app/main.py#L64) `verify_api_key()` ตรวจแค่ว่า key นี้มีอยู่จริงและยัง active
จากนั้น [main.py:81](app/main.py#L81) `_resolve_and_enqueue()` ไปหา `event_type` จาก
`code` ที่ส่งมา **โดยไม่เช็คว่า event type นั้นเป็นของเจ้าของ key คนนั้นหรือไม่**

ผลในแบบ B: **ลูกค้า A ที่รู้ code ของลูกค้า B สามารถสั่งให้ระบบเราโทรหาเบอร์ของลูกค้า B ได้**
ทั้งเปลืองค่าโทรของเรา และเป็นการรบกวนคนอื่นที่ไม่เกี่ยวข้อง
ในแบบ A เรื่องนี้ไม่มีผลเพราะ key ทุกอันเป็นของเราเองอยู่แล้ว

### 4.2 `code` และชื่อกลุ่มเป็น unique ทั้งระบบ

- [database.py:105](app/database.py#L105) — `event_types.code` เป็น `unique=True`
- [database.py:76](app/database.py#L76) — `groups.name` เป็น `unique=True`

ในแบบ B ลูกค้า 2 รายอยากใช้ code `power_outage` และตั้งกลุ่มชื่อ "ทีมช่าง" เหมือนกันเป็นเรื่องปกติ
ตอนนี้รายที่สองจะสร้างไม่ได้ ได้ 409 กลับไป — ต้องเปลี่ยนเป็น `UNIQUE(tenant_id, code)`

### 4.3 config เป็น singleton ทั้งระบบ

[database.py:61](app/database.py#L61) — `app_settings` มีแถวเดียวเสมอ (`id=1`)
retry/timeout/SMS fallback จึงเป็นค่าเดียวใช้ร่วมกันหมด

ในแบบ B ลูกค้าที่จ่ายแพงกว่าอาจต้องการ retry มากกว่า หรือบางรายไม่อยากให้ส่ง SMS เลย
ค่าพวกนี้ต้องย้ายไปอยู่ระดับ tenant

### 4.4 คิวเป็น FIFO ล้วน — ไม่มีความเป็นธรรมระหว่าง tenant

[queue_manager.py:37](app/queue_manager.py#L37) เรียงตาม `created_at` เท่านั้น

ในแบบ B ถ้าลูกค้า A มีโหนดพังรวดเดียว 50 จุด ยิงเข้ามา 50 งาน
งานของลูกค้า B ที่เข้ามาทีหลังจะรอจนกว่า 50 สายนั้นจะโทรเสร็จ
ด้วยเวลา ring timeout 25 วิ + retry คือรอเป็น**หลายสิบนาที** สำหรับเหตุฉุกเฉิน

ต้องเปลี่ยนเป็นวนหยิบแบบ round-robin ข้าม tenant + จำกัดจำนวนงานค้างต่อ tenant

### 4.5 ไม่มีการวัดว่าใครใช้ไปเท่าไหร่

`call_jobs` และ `call_logs` ไม่มีคอลัมน์บอกเจ้าของ ([database.py:172](app/database.py#L172)
`call_logs.job_id` ยังไม่มี FK ด้วยซ้ำ) จึงตอบไม่ได้ว่าเดือนนี้ลูกค้ารายไหนใช้ไปกี่สาย กี่นาที

ในแบบ B นี่คือข้อมูลที่ต้องใช้ทั้งการคิดเงินและการจำกัดโควตา — และค่าโทรเป็นเงินจริงที่เราจ่ายออกไปก่อน

### 4.6 Dashboard มีผู้ใช้คนเดียวจาก `.env`

[main.py:124](app/main.py#L124) เทียบ username กับ `settings.admin_username` และรหัสผ่านกับ
bcrypt hash ใน `.env` ([config.py:29-30](app/config.py#L29-L30)) ไม่มีตาราง user

- แบบ B1 ไม่ต้องแก้เลย (มีแต่เราที่ login)
- แบบ B2 ต้องรื้อทำใหม่ทั้งส่วน auth + ใส่การกรอง tenant ทุก endpoint

เกี่ยวกันด้วย: [main.py:57](app/main.py#L57) CORS อนุญาต origin เดียวจาก `.env`
ถ้าลูกค้าจะเรียกจากหน้าเว็บของตัวเองต้องรองรับหลาย origin

---

## 5. ข้อจำกัดที่ตัดสินทุกอย่าง: ซิมใบเดียว

เรื่องนี้สำคัญกว่าทุกข้อในหมวด 4 เพราะแก้ด้วยโค้ดไม่ได้

**ฮาร์ดแวร์โทรได้ทีละ 1 สาย** งานทุกชิ้นต้องต่อคิวเรียงกัน
สมมติ ring timeout 25 วิ + retry 2 ครั้ง + escalate 3 เบอร์ = 1 เหตุการณ์ที่ไม่มีใครรับสาย
กินเวลาระดับหลายนาที ก่อนจะถึงงานถัดไป

| | แบบ A | แบบ B |
|---|---|---|
| คิวยาว = อะไร | งานเราเองช้า เรารับได้/จัดลำดับเองได้ | ลูกค้าไม่ได้รับแจ้งเตือนเหตุฉุกเฉินตามที่ตกลง |
| ยอมรับได้ไหม | ได้ | **ไม่ได้** ถ้าจะรับเงินเขา |

**แบบ B ในระดับที่ใช้งานจริงต้องมีช่องสัญญาณมากกว่า 1** ทางเลือก:

1. **หลายโมดูล + หลายซิม** — โปรเจคนี้มี branch `feature/voip-multi-sim` ที่ทำ multi-SIM pool ไว้แล้ว
   ซึ่งถูกตัดออกจาก `main` เพื่อความเรียบง่าย นี่คือทางที่ตรงที่สุดถ้าจะไปแบบ B
2. **VoIP trunk** — โทรออกพร้อมกันได้หลายสายตามที่ผู้ให้บริการอนุญาต ไม่ติดเพดานฮาร์ดแวร์
   branch เดียวกันมี Asterisk + Zadarma อยู่แล้ว
3. **จำกัด SLA ตามความจริง** — ประกาศชัดว่ารับได้กี่ tenant กี่ event ต่อชั่วโมง แล้วบังคับด้วย quota

---

## 6. เรื่องที่ไม่ใช่โค้ด แต่ตัดสินว่าแบบ B ทำได้หรือไม่

หัวข้อพวกนี้ผมไม่สามารถยืนยันรายละเอียดทางกฎหมายให้ได้ **ต้องไปตรวจสอบจากแหล่งจริง**
แต่ต้องรู้ไว้ก่อนตัดสินใจ เพราะบางข้ออาจทำให้แบบ B ไม่คุ้มเลย

| เรื่อง | ประเด็นที่ต้องเช็ค |
|---|---|
| เงื่อนไขการใช้ซิม | ซิม GOMO เป็นแพ็กเกจผู้บริโภค การเอามาโทรออกอัตโนมัติเชิงพาณิชย์แทนบุคคลที่สาม มักขัดกับเงื่อนไขการใช้งาน/fair use และเสี่ยงถูกระงับเลขหมาย |
| การให้บริการโทรคมนาคม | การรับเงินเพื่อให้บริการโทรออกแทนผู้อื่นอาจเข้าข่ายกิจการที่ต้องได้รับอนุญาตจาก กสทช. |
| PDPA | แบบ A เราเป็นเจ้าของข้อมูลพนักงานเราเอง แบบ B เราถือเบอร์พนักงานบริษัทอื่น = เป็นผู้ประมวลผลข้อมูล ต้องมีข้อตกลงการประมวลผล สิทธิขอลบ และการแจ้งเหตุข้อมูลรั่ว |
| ความยินยอมของผู้ถูกโทร | คนที่ปลายสายเป็นพนักงานลูกค้า ไม่ได้ตกลงอะไรกับเราโดยตรง ใครรับผิดชอบเรื่องความยินยอม |
| ความรับผิดเมื่อสายไม่ถึง | ถ้าระบบล่มแล้วลูกค้าไม่ได้รับแจ้งเตือนจนเกิดความเสียหาย ขอบเขตความรับผิดอยู่ที่ใคร ต้องเขียนใน SLA |

**สำหรับโปรเจคสหกิจ:** ถ้าจะนำเสนอแบบ B ควรนำเสนอเป็น "สถาปัตยกรรมที่รองรับ multi-tenant"
พร้อมสาธิตด้วย tenant สมมติ 2 ราย — ไม่ใช่การเปิดให้บุคคลภายนอกใช้จริงโดยรับเงิน
จะได้แสดงความสามารถทางเทคนิคโดยไม่ต้องแบกประเด็นข้างบนทั้งหมด

---

## 7. ควรทำอะไรตอนนี้

หลักการ: **ทำสิ่งที่ตอนนี้ถูก แต่ทีหลังแพง** ส่วนที่ทีหลังทำก็ราคาเท่ากัน ปล่อยไว้ก่อน

### ทำตอนนี้ (ถูกเพราะตารางยังว่าง)

| งาน | ทำไมต้องตอนนี้ |
|---|---|
| ตาราง `tenants` + `tenant_id` ใน `api_keys`, `groups`, `event_types`, `call_jobs`, `call_logs` | ทีหลังต้องแก้ทุก query + backfill ข้อมูลที่ไม่รู้เจ้าของ |
| เปลี่ยน unique เป็น `UNIQUE(tenant_id, code)` และ `UNIQUE(tenant_id, name)` | เป็น constraint ระดับ schema แก้ทีหลังต้องสร้างตารางใหม่ทั้งใบ |
| `verify_api_key` คืน tenant ของ key นั้น แล้ว `/notify` ตรวจว่า event type เป็นของ tenant เดียวกัน | เป็นช่องโหว่ความปลอดภัย ยิ่งเขียน endpoint เพิ่มยิ่งต้องตามแก้หลายที่ |
| แบบ A = สร้าง tenant แถวเดียวชื่อ "internal" | ทำให้โค้ดเส้นเดียวรองรับทั้งสองแบบ ไม่ต้องมี if แยก |

ต้นทุนรวม: 1 migration + แก้ query ที่ service layer + endpoint `/notify` ทำได้ในรอบเดียว

### รอได้ (ทีหลังราคาเท่าเดิม)

- ตาราง `users` + login แยกรายลูกค้า + role → รอจนตัดสินใจว่าจะไป B2
- Quota / rate limit ต่อ tenant → เพิ่มได้ทุกเมื่อเมื่อมี `tenant_id` แล้ว
- การคิดเงิน/รายงานการใช้งาน → เป็น query บนข้อมูลที่เก็บไว้แล้ว
- คิวแบบ round-robin ข้าม tenant → แก้ที่ `claim_next_job()` ที่เดียว
- หลาย origin ใน CORS → ค่า config

### ไม่ควรทำจนกว่าจะชัดเจน

- multi-SIM / VoIP — ต้องรู้ก่อนว่าจะรับ tenant กี่รายจริง และ branch `feature/voip-multi-sim` มีอยู่แล้ว
  ไม่ต้องเขียนใหม่

---

## 8. ตารางสรุป: อะไรต้องทำในแบบไหน

| งาน | แบบ A | แบบ B1 (managed) | แบบ B2 (self-service) |
|---|---|---|---|
| P0/P1 เดิม (escalation, stream_audio, error handling) | ต้องทำ | ต้องทำ | ต้องทำ |
| ตาราง `tenants` + `tenant_id` | มี 1 แถว | ต้องทำ | ต้องทำ |
| unique ต่อ tenant | ไม่จำเป็นแต่ไม่เสียหาย | ต้องทำ | ต้องทำ |
| `/notify` ตรวจ tenant ของ key | ไม่จำเป็น | **ต้องทำ (ความปลอดภัย)** | ต้องทำ |
| config แยกต่อ tenant | ไม่ต้อง | ควรมี | ต้องทำ |
| quota / rate limit ต่อ tenant | ควรมี (กัน bug ยิงซ้ำ) | ต้องทำ | ต้องทำ |
| คิว round-robin | ไม่ต้อง | ต้องทำ | ต้องทำ |
| วัดการใช้งานต่อ tenant | ไม่ต้อง | ต้องทำ | ต้องทำ |
| ตาราง `users` + role | ไม่ต้อง | ไม่ต้อง | ต้องทำ |
| กรอง tenant ทุก endpoint | ไม่ต้อง | ไม่ต้อง | ต้องทำ |
| หลายช่องสัญญาณ (multi-SIM/VoIP) | ไม่ต้อง | ตามจำนวนลูกค้า | ต้องทำ |
| PDPA / ข้อตกลงกับลูกค้า / SLA | ไม่ต้อง | ต้องมี | ต้องมี |
