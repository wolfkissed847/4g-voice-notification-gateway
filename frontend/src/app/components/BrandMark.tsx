/**
 * BrandMark — โลโก้ของระบบ ใช้ตัวเดียวกันทุกที่ (หน้า login, แถบเมนู, ไอคอนแท็บเบราว์เซอร์)
 *
 * ── ทำไมเป็น component ไม่ใช่ไฟล์รูป ──────────────────────────────────────
 * มันต้องเปลี่ยนสีตามที่ที่มันไปอยู่: บนแถบเมนูใช้สีของธีม (เปลี่ยนตาม light/dark)
 * ส่วนบนแผงภาพหน้า login ใช้สีฟ้าตายตัวของแผงนั้น ถ้าเป็น .png/.svg ไฟล์เดียว
 * จะได้สีเดียวตายตัว ต้องทำหลายไฟล์แล้วคอยจำว่าอันไหนใช้ที่ไหน
 * (ไฟล์ favicon.svg ใน public/ เป็นสำเนาที่ตรึงสีไว้ — เบราว์เซอร์ไม่ยอมรับ component
 *  ถ้าแก้รูปทรงตรงนี้ ต้องไปแก้ไฟล์นั้นด้วยให้ตรงกัน)
 *
 * ── รูปทรง ────────────────────────────────────────────────────────────────
 * ลูกโลกที่มีเส้นเมริเดียน + คลื่นสัญญาณแผ่ออกจากจุดบนผิวโลก
 * = "โทรจากที่นี่ไปถึงคนที่อยู่ที่ไหนก็ได้" ซึ่งเป็นสิ่งที่ระบบนี้ทำจริง
 * ไม่ใช้ตัวอักษร "4G" เป็นโลโก้ เพราะที่ขนาดไอคอนแท็บ (16px) ตัวอักษรสองตัวจะอ่านไม่ออก
 * เหลือเป็นก้อนสีทึบ ส่วนรูปทรงยังพอแยกออกว่าเป็นอะไร
 */
export function BrandMark({ size = 24, className }: { size?: number | string; className?: string }) {
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.6"
      strokeLinecap="round"
      strokeLinejoin="round"
      className={className}
      aria-hidden
    >
      {/* ตัวโลก */}
      <circle cx="11" cy="12.5" r="7.5" />
      {/* เส้นศูนย์สูตร + เมริเดียน — สองเส้นนี้คือสิ่งที่ทำให้วงกลมอ่านเป็น "ทรงกลม" */}
      <path d="M3.5 12.5h15" />
      <ellipse cx="11" cy="12.5" rx="3.4" ry="7.5" />
      {/* จุดส่งสัญญาณบนผิวโลก + คลื่นแผ่ออกสองชั้น */}
      <circle cx="16.4" cy="7.4" r="1.1" fill="currentColor" stroke="none" />
      <path d="M18.4 5.6a3.4 3.4 0 0 1 0 3.6" />
      <path d="M20.4 3.9a6.4 6.4 0 0 1 0 7" />
    </svg>
  );
}
