fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1
sqlalchemy>=2.0.29
alembic>=1.13.1
pyserial>=3.5
gTTS>=2.5.1
python-multipart>=0.0.9
pyjwt>=2.8.0
bcrypt>=4.1.2
cryptography>=42.0.0
pre-commit>=3.7.0
psutil>=5.9.8
# คุมขา PWRKEY/STATUS ของโมดูลที่ต่อผ่านหัว GPIO — ใช้เฉพาะบน Linux (Raspberry Pi)
# จำกัดด้วย platform marker เพราะ lgpio build บน Windows ไม่ได้ ถ้าไม่กันไว้
# เครื่องพัฒนาที่ตั้ง venv ใหม่จะ pip install ทั้งไฟล์ไม่ผ่านตั้งแต่ต้น
# ฝั่งโค้ด import แบบ lazy ใน gsm_power.py อยู่แล้ว ไม่มีไลบรารีก็ยังรันได้ (แค่คุมขาไม่ได้)
lgpio>=0.2.2.0; platform_system == "Linux"

